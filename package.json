{
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "scripts": {
    "prepare": "husky",
    "format:terraform": "terraform fmt -recursive"
  },
  "lint-staged": {
    "**/*.tf": "npm run format:terraform --"
  }
}
